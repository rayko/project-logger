.mb-2
  = form_with(model: todo_item, class: 'row') do |form|
    = form.hidden_field :project_id
    .col
      = form.text_field :text, :class => 'form-control', placeholder: 'Add item ...'
    .col-2
      = form.submit 'Add', :class => 'btn btn-outline-primary'
